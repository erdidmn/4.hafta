use DataWarehouse
go
create table date_dimensionss(
day_number int primary key,
day_date date,
week_number int,
month_number int,
year_number int);

create table customer_dimensionss(
customer_İd int primary key,
customer_name varchar(30),
customer_adress varchar(50),
other_details char);

create table Product_Dimensionss(
product_code int primary key,
product_name char,
product_description varchar(50),
other_product_details varchar(50));

create table Data_Warehouse_Factsss(
fact_id int primary key,
customer_id int,
day_number int,
geographic_area int,
product_code int,
fact_name char,
fact_description varchar(50),
fact_amount int,
fact_count int,
other_fact_details varchar(50));

create table Geographic_Dimensionss(
geographic_area int,
geographic_area_name char,
region varchar(20),
country char,
other_region_details varchar(50));
